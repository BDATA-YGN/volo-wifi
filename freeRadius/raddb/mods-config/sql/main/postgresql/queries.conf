# -*- text -*-
#
#  main/postgresql/queries.conf -- PostgreSQL configuration for default schema (schema.sql)
#
#  $Id: 80953e0c4e5577a4eeeb3dd98e73a967eb38f52e $

# Use the driver specific SQL escape method.
#
# If you enable this configuration item, the "safe_characters"
# configuration is ignored.  FreeRADIUS then uses the PostgreSQL escape
# functions to escape input strings.  The only downside to making this
# change is that the PostgreSQL escaping method is not the same the one
# used by FreeRADIUS.  So characters which are NOT in the
# "safe_characters" list will now be stored differently in the database.
#
#auto_escape = yes

# Safe characters list for sql queries. Everything else is replaced
# with their mime-encoded equivalents.
# The default list should be ok
# Using 'auto_escape' is preferred
# safe_characters = "@abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789.-_: /"

#######################################################################
#  Query config:  Username
#######################################################################
# This is the username that will get substituted, escaped, and added
# as attribute 'SQL-User-Name'.  '%{SQL-User-Name}' should be used
# below everywhere a username substitution is needed so you you can
# be sure the username passed from the client is escaped properly.
#
# Uncomment the next line, if you want the sql_user_name to mean:
#
#    Use Stripped-User-Name, if it's there.
#    Else use User-Name, if it's there,
#    Else use hard-coded string "none" as the user name.
#
#sql_user_name = "%{%{Stripped-User-Name}:-%{%{User-Name}:-none}}"

sql_user_name = "%{User-Name}"

#######################################################################
#  Query config:  Event-Timestamp
#######################################################################
# event_timestamp_epoch is the basis for the time inserted into
# accounting records. Typically this will be the Event-Timestamp of the
# accounting request, which is usually provided by a NAS.
#
# Uncomment the next line, if you want the timestamp to be based on the
# request reception time recorded by this server, for example if you
# distrust the provided Event-Timestamp.
#event_timestamp_epoch = "%l"

event_timestamp_epoch = "%{%{integer:Event-Timestamp}:-%l}"

# event_timestamp is the SQL snippet for converting an epoch timestamp
# to an SQL date.

event_timestamp = "TO_TIMESTAMP(${event_timestamp_epoch})"

#######################################################################
#  Query config:  Class attribute
#######################################################################
#
#  3.0.22 and later have a "class" column in the accounting table.
#
#  However, we do NOT want to break existing configurations by adding
#  the Class attribute to the default queries.  If we did that, then
#  systems using newer versions of the server would fail, because
#  there is no "class" column in their accounting tables.
#
#  The solution to that is the following "class" subsection.  If your
#  database has a "class" column for the various tables, then you can
#  uncomment the configuration items here.  The queries below will
#  then automatically insert the Class attribute into radacct,
#  radpostauth, etc.
#
class {
	#
	#  Delete the '#' character from each of the configuration
	#  items in this section.  This change puts the Class
	#  attribute into the various tables.  Leave the double-quoted
	#  string there, as the value for the configuration item.
	#
	#  See also policy.d/accounting, and the "insert_acct_class"
	#  policy.  You will need to list (or uncomment)
	#  "insert_acct_class" in the "post-auth" section in order to
	#  create a Class attribute.
	#
	column_name =	# ", Class"
	packet_xlat =	# ", '%{Class}'"
	reply_xlat =	# ", '%{reply:Class}'"
}

#######################################################################
#  Default profile
#######################################################################
# This is the default profile. It is found in SQL by group membership.
# That means that this profile must be a member of at least one group
# which will contain the corresponding check and reply items.
# This profile will be queried in the authorize section for every user.
# The point is to assign all users a default profile without having to
# manually add each one to a group that will contain the profile.
# The SQL module will also honor the User-Profile attribute. This
# attribute can be set anywhere in the authorize section (ie the users
# file). It is found exactly as the default profile is found.
# If it is set then it will *overwrite* the default profile setting.
# The idea is to select profiles based on checks on the incoming
# packets, not on user group membership. For example:
# -- users file --
# DEFAULT	Service-Type == Outbound-User, User-Profile := "outbound"
# DEFAULT	Service-Type == Framed-User, User-Profile := "framed"
#
# By default the default_user_profile is not set
#
# default_user_profile = "DEFAULT"

#######################################################################
#  Open Query
#######################################################################
# This query is run whenever a new connection is opened.
# It is commented out by default.
#
# If you have issues with connections hanging for too long, uncomment
# the next line, and set the timeout in milliseconds.  As a general
# rule, if the queries take longer than a second, something is wrong
# with the database.
#open_query = "set statement_timeout to 1000"

#######################################################################
#  NAS Query
#######################################################################
#  This query retrieves the radius clients
#
#  0. Row ID (currently unused)
#  1. Name (or IP address)
#  2. Shortname
#  3. Type
#  4. Secret
#  5. Server
#######################################################################

client_query = "\
	SELECT id, nasname, shortname, type, secret, server \
	FROM ${client_table}"

#######################################################################
#  Authorization Queries
#######################################################################
#  These queries compare the check items for the user
#  in ${authcheck_table} and setup the reply items in
#  ${authreply_table}.  You can use any query/tables
#  you want, but the return data for each row MUST
#  be in the  following order:
#
#  0. Row ID (currently unused)
#  1. UserName/GroupName
#  2. Item Attr Name
#  3. Item Attr Value
#  4. Item Attr Operation
#######################################################################

#
#  Direct table lookups (indexed token/username). Avoid radcheck/radreply views
#  on authorize — those views scan all credentials and are very slow at scale.
#
authorize_check_query = "\
	SELECT id, username AS UserName, attribute AS Attribute, value AS Value, op AS Op \
	FROM ( \
		SELECT \
			1 AS sort_key, \
			c.id AS id, \
			COALESCE(c.username, c.token) AS username, \
			CASE \
				WHEN c.username IS NOT NULL AND c.\"passwordHash\" IS NOT NULL THEN 'Crypt-Password' \
				WHEN c.token IS NOT NULL THEN 'Cleartext-Password' \
			END AS attribute, \
			':=' AS op, \
			CASE \
				WHEN c.username IS NOT NULL AND c.\"passwordHash\" IS NOT NULL THEN c.\"passwordHash\" \
				WHEN c.token IS NOT NULL THEN c.token \
			END AS value \
		FROM wf_credential c \
		WHERE (c.token = '%{SQL-User-Name}' OR c.username = '%{SQL-User-Name}') \
		AND (c.\"expiresAt\" IS NULL OR c.\"expiresAt\" > CURRENT_TIMESTAMP) \
		AND c.\"status\" IN ('SOLD', 'ACTIVE', 'ACTIVATED', 'IN_USE') \
		AND c.\"deletedAt\" IS NULL \
		AND ( \
			(c.username IS NOT NULL AND c.\"passwordHash\" IS NOT NULL) \
			OR c.token IS NOT NULL \
		) \
		UNION ALL \
		SELECT \
			2 AS sort_key, \
			c.id AS id, \
			COALESCE(c.username, c.token) AS username, \
			'Simultaneous-Use' AS attribute, \
			':=' AS op, \
			COALESCE(p.\"maxDevices\", 1)::text AS value \
		FROM wf_credential c \
		INNER JOIN wf_plan p ON c.\"planId\" = p.id AND p.\"deletedAt\" IS NULL \
		WHERE (c.token = '%{SQL-User-Name}' OR c.username = '%{SQL-User-Name}') \
		AND (c.\"expiresAt\" IS NULL OR c.\"expiresAt\" > CURRENT_TIMESTAMP) \
		AND c.\"status\" IN ('SOLD', 'ACTIVE', 'ACTIVATED', 'IN_USE') \
		AND c.\"deletedAt\" IS NULL \
	) combined \
	ORDER BY sort_key, id"

authorize_reply_query = "\
	SELECT \
		ROW_NUMBER() OVER (ORDER BY deduped.priority ASC, deduped.pra_id)::integer AS id, \
		deduped.UserName, \
		deduped.Attribute, \
		deduped.Value, \
		deduped.Op \
	FROM ( \
		SELECT DISTINCT ON (pra.\"attributeName\") \
			COALESCE(c.username, c.token) AS UserName, \
			pra.\"attributeName\" AS Attribute, \
			CASE \
				WHEN pra.op IN (':=','=','==','+=','-=','!=','>','>=','<','<=','=~','!~','=*','!*') THEN pra.op \
				ELSE ':=' \
			END AS Op, \
			CASE \
				WHEN (pra.value IS NULL OR pra.value = '') \
					AND pra.op NOT IN (':=','=','==','+=','-=','!=','>','>=','<','<=','=~','!~','=*','!*') \
					THEN pra.op \
				WHEN pra.value LIKE '%%{timeSeconds}%%' THEN \
					REPLACE( \
						pra.value, \
						'{timeSeconds}', \
						COALESCE( \
							c.\"timeRemainingSec\"::text, \
							(p.\"timeAmount\" * \
								CASE p.\"timeUnit\" \
									WHEN 'MINUTE' THEN 60 \
									WHEN 'HOUR' THEN 3600 \
									WHEN 'DAY' THEN 86400 \
									ELSE 0 \
								END)::text, \
							'0' \
						) \
					) \
				WHEN pra.value LIKE '%%{dataMb}%%' THEN \
					REPLACE( \
						pra.value, \
						'{dataMb}', \
						COALESCE(c.\"dataRemainingMb\"::text, p.\"dataMb\"::text, '0') \
					) \
				ELSE pra.value \
			END AS Value, \
			pra.priority AS priority, \
			pra.id AS pra_id \
		FROM wf_credential c \
		INNER JOIN wf_plan p ON p.id = c.\"planId\" AND p.\"deletedAt\" IS NULL \
		LEFT JOIN wf_station ws ON ws.id = c.\"stationId\" AND ws.\"deletedAt\" IS NULL \
		INNER JOIN wf_plan_radius_attribute pra ON pra.\"planId\" = p.id \
			AND pra.phase = 'REPLY' \
			AND pra.\"deletedAt\" IS NULL \
			AND ( \
				pra.\"wifiStationId\" IS NULL \
				OR (c.\"stationId\" IS NOT NULL AND pra.\"wifiStationId\" = c.\"stationId\") \
			) \
		WHERE (c.token = '%{SQL-User-Name}' OR c.username = '%{SQL-User-Name}') \
		AND (c.\"expiresAt\" IS NULL OR c.\"expiresAt\" > CURRENT_TIMESTAMP) \
		AND c.\"deletedAt\" IS NULL \
		ORDER BY pra.\"attributeName\", pra.priority ASC, pra.id \
	) deduped \
	ORDER BY deduped.priority ASC, deduped.pra_id"

# authorize_check_query = "\
# 	SELECT \
# 		id, \
# 		COALESCE(token, username) as username, \
# 		CASE \
# 			WHEN type = 'VOUCHER_TOKEN' THEN 'Cleartext-Password' \
# 			WHEN \"passwordHash\" IS NOT NULL THEN 'Crypt-Password' \
# 			ELSE 'Cleartext-Password' \
# 		END as attribute, \
# 		CASE \
# 			WHEN type = 'VOUCHER_TOKEN' THEN token \
# 			ELSE \"passwordHash\" \
# 		END as value, \
# 		':=' as op \
# 	FROM wf_credential \
# 	WHERE (token = '%{SQL-User-Name}' OR username = '%{SQL-User-Name}') \
# 	AND status IN ('SOLD', 'ACTIVATED') \
# 	AND (\"expiresAt\" IS NULL OR \"expiresAt\" > CURRENT_TIMESTAMP) \
# 	AND \"revokedAt\" IS NULL \
# 	AND \"deletedAt\" IS NULL \
# 	ORDER BY CASE WHEN token = '%{SQL-User-Name}' THEN 0 ELSE 1 END, id \
# 	LIMIT 1"

# authorize_reply_query = "\
# 	SELECT \
# 		id, \
# 		COALESCE(token, username) as username, \
# 		'Service-Type' as attribute, \
# 		'Framed-User' as value, \
# 		':=' as op \
# 	FROM wf_credential \
# 	WHERE (token = '%{SQL-User-Name}' OR username = '%{SQL-User-Name}') \
# 	AND status IN ('SOLD', 'ACTIVATED') \
# 	AND (\"expiresAt\" IS NULL OR \"expiresAt\" > CURRENT_TIMESTAMP) \
# 	AND \"revokedAt\" IS NULL \
# 	AND \"deletedAt\" IS NULL \
# 	ORDER BY CASE WHEN token = '%{SQL-User-Name}' THEN 0 ELSE 1 END, id \
# 	LIMIT 1"

#
#  Use these for case insensitive usernames. WARNING: Slower queries!
#
#authorize_group_check_query = "\
#	SELECT \
#		${groupcheck_table}.id, ${groupcheck_table}.GroupName, ${groupcheck_table}.Attribute, \
#		${groupcheck_table}.Value, ${groupcheck_table}.Op \
#	FROM ${groupcheck_table}, ${usergroup_table} \
#	WHERE LOWER(${usergroup_table}.UserName) = LOWER('%{SQL-User-Name}') \
#	AND ${usergroup_table}.GroupName = ${groupcheck_table}.GroupName \
#	ORDER BY ${groupcheck_table}.id"

#authorize_group_reply_query = "\
#	SELECT \
#		${groupreply_table}.id, ${groupreply_table}.GroupName, \
#		${groupreply_table}.Attribute, ${groupreply_table}.Value, ${groupreply_table}.Op \
#	FROM ${groupreply_table}, ${usergroup_table} \
#	WHERE LOWER(${usergroup_table}.UserName) = LOWER('%{SQL-User-Name}') \
#	AND ${usergroup_table}.GroupName = ${groupreply_table}.GroupName \
#	ORDER BY ${groupreply_table}.id"

authorize_group_check_query = "\
	SELECT \
		ROW_NUMBER() OVER (ORDER BY deduped.priority ASC, deduped.pra_id)::integer AS id, \
		deduped.GroupName, \
		deduped.Attribute, \
		deduped.Value, \
		deduped.Op \
	FROM ( \
		SELECT DISTINCT ON (pra.\"attributeName\") \
			p.code || COALESCE(c.username, c.token) AS GroupName, \
			pra.\"attributeName\" AS Attribute, \
			CASE \
				WHEN pra.op IN (':=','=','==','+=','-=','!=','>','>=','<','<=','=~','!~','=*','!*') THEN pra.op \
				ELSE '==' \
			END AS Op, \
			pra.value AS Value, \
			pra.priority AS priority, \
			pra.id AS pra_id \
		FROM wf_credential c \
		INNER JOIN wf_plan p ON p.id = c.\"planId\" AND p.\"deletedAt\" IS NULL \
		LEFT JOIN wf_station ws ON ws.id = c.\"stationId\" AND ws.\"deletedAt\" IS NULL \
		INNER JOIN wf_plan_radius_attribute pra ON pra.\"planId\" = p.id \
			AND pra.phase = 'CHECK' \
			AND pra.\"deletedAt\" IS NULL \
			AND ( \
				pra.\"wifiStationId\" IS NULL \
				OR (c.\"stationId\" IS NOT NULL AND pra.\"wifiStationId\" = c.\"stationId\") \
			) \
			AND ( \
				c.\"stationId\" IS NULL \
				OR ws.\"radiusVendorProfileId\" IS NULL \
				OR pra.\"vendorProfileId\" IS NULL \
				OR pra.\"vendorProfileId\" = ws.\"radiusVendorProfileId\" \
			) \
		WHERE (c.token = '%{SQL-User-Name}' OR c.username = '%{SQL-User-Name}') \
		AND (c.\"expiresAt\" IS NULL OR c.\"expiresAt\" > CURRENT_TIMESTAMP) \
		AND c.\"deletedAt\" IS NULL \
		AND (p.code || COALESCE(c.username, c.token)) = '%{${group_attribute}}' \
		ORDER BY pra.\"attributeName\", pra.priority ASC, pra.id \
	) deduped \
	ORDER BY deduped.priority ASC, deduped.pra_id"

# REPLY attributes are already merged via authorize_reply_query (radreply / wf_plan_radius_attribute
# phase REPLY per user). radgroupreply is an intentionally empty view; querying it still costs
# ~3s on production if the DB view was not recreated. Return zero rows without touching the view.
authorize_group_reply_query = "\
	SELECT \
		NULL::integer AS id, \
		NULL::varchar(128) AS GroupName, \
		NULL::varchar(64) AS Attribute, \
		NULL::varchar(253) AS Value, \
		NULL::varchar(2) AS op \
	WHERE false"

#######################################################################
# Simultaneous Use Checking Queries
#######################################################################
# simul_count_query     - query for the number of current connections
#                       - If this is not defined, no simultaneous use checking
#                       - will be performed by this module instance
# simul_verify_query    - query to return details of current connections for verification
#                       - Leave blank or commented out to disable verification step
#                       - Note that the returned field order should not be changed.
#######################################################################

simul_count_query = "\
	SELECT COUNT(id) \
	FROM wf_radius_session a \
	WHERE \"userName\"='%{SQL-User-Name}' \
	AND \"stoppedAt\" IS NULL "

simul_verify_query = "\
	SELECT id, \"acctSessionId\", \"userName\", \"nasIpAddress\", '0', \"framedIpAddress\", \"callingStationId\", \
		'prot' \
	FROM wf_radius_session a \
	WHERE \"userName\"='%{SQL-User-Name}' \
	AND \"stoppedAt\" IS NULL "

#######################################################################
# Group Membership Queries
#######################################################################
# group_membership_query        - Check user group membership
#######################################################################

# Use these for case insensitive usernames. WARNING: Slower queries!
#group_membership_query = "\
#	SELECT GroupName \
#	FROM ${usergroup_table} \
#	WHERE LOWER(UserName) = LOWER('%{SQL-User-Name}') \
#	ORDER BY priority"

group_membership_query = "\
	SELECT p.code || COALESCE(c.username, c.token) AS groupname \
	FROM wf_credential c \
	INNER JOIN wf_plan p ON p.id = c.\"planId\" AND p.\"deletedAt\" IS NULL \
	WHERE (c.token = '%{SQL-User-Name}' OR c.username = '%{SQL-User-Name}') \
	AND (c.username IS NOT NULL OR c.token IS NOT NULL) \
	AND (c.\"expiresAt\" IS NULL OR c.\"expiresAt\" > CURRENT_TIMESTAMP) \
	AND c.\"deletedAt\" IS NULL \
	ORDER BY 1"

#######################################################################
# Accounting and Post-Auth Queries
#######################################################################
# These queries insert/update accounting and authentication records.
# The query to use is determined by the value of 'reference'.
# This value is used as a configuration path and should resolve to one
# or more 'query's. If reference points to multiple queries, and a query
# fails, the next query is executed.
#
# Behaviour is identical to the old 1.x/2.x module, except we can now
# fail between N queries, and query selection can be based on any
# combination of attributes, or custom 'Acct-Status-Type' values.
#######################################################################

accounting {
	reference = "%{tolower:type.%{%{Acct-Status-Type}:-%{Request-Processing-Stage}}.query}"

	#
	# wf_radius_session: one row per acctSessionId + nasIpAddress (unique index required).
	# UPDATE open rows first; INSERT fallback uses ON CONFLICT DO UPDATE.
	# Stop UPDATE is idempotent (no stoppedAt filter).
	# Write SQL queries to a logfile. This is potentially useful for bulk inserts
	# when used with the rlm_sql_null driver.
#	logfile = ${logdir}/accounting.sql

	column_list = "\
		AcctSessionId, \
		AcctUniqueId, \
		UserName, \
		Realm, \
		NASIPAddress, \
		NASPortId, \
		NASPortType, \
		AcctStartTime, \
		AcctUpdateTime, \
		AcctStopTime, \
		AcctSessionTime, \
		AcctAuthentic, \
		ConnectInfo_start, \
		ConnectInfo_Stop, \
		AcctInputOctets, \
		AcctOutputOctets, \
		CalledStationId, \
		CallingStationId, \
		AcctTerminateCause, \
		ServiceType, \
		FramedProtocol, \
		FramedIpAddress, \
		FramedIpv6Address, \
		FramedIpv6Prefix, \
		FramedInterfaceId, \
		DelegatedIpv6Prefix \
		${..class.column_name}"

	type {

		accounting-on {

			#
			#  "Bulk update" Accounting-On/Off strategy.
			#
			#  Immediately terminate all sessions associated with a
			#  given NAS.
			#
			#  Note: If a large number of sessions require closing
			#  then the bulk update may be take a long time to run
			#  and lock an excessive number of rows. See the
			#  strategy below for an alternative approach that does
			#  not touch the radacct session data.
			#
			query = "\
				UPDATE ${....acct_table1} \
				SET \
					AcctStopTime = ${....event_timestamp}, \
					AcctUpdateTime = ${....event_timestamp}, \
					AcctSessionTime = (${....event_timestamp_epoch} - EXTRACT(EPOCH FROM(AcctStartTime))), \
					AcctTerminateCause = '%{%{Acct-Terminate-Cause}:-NAS-Reboot}' \
				WHERE AcctStopTime IS NULL \
				AND NASIPAddress= '%{%{NAS-IPv6-Address}:-%{NAS-IP-Address}}' \
				AND AcctStartTime <= ${....event_timestamp}"

			#
			#  "Lightweight" Accounting-On/Off strategy.
			#
			#  Record the reload time of the NAS and let the
			#  administrator actually close the sessions in radacct
			#  out-of-band, if desired.
			#
			#  Implementation advice, together with a stored
			#  procedure for closing sessions and a view showing
			#  the effective stop time of each session is provided
			#  in process-radacct.sql.
			#
			#  To enable this strategy, just change the previous
			#  query to "-query", and this one to "query".  The
			#  previous one will be ignored, and this one will be
			#  enabled.
			#
			-query = "\
				INSERT INTO nasreload (NASIPAddress, ReloadTime) \
				VALUES ('%{NAS-IP-Address}', ${....event_timestamp}) \
				ON CONFLICT ON (NASIPAddress) \
				DO UPDATE SET \
					ReloadTime = ${....event_timestamp}"

		}

		accounting-off {
			query = "${..accounting-on.query}"
		}

		#
		#  Implement the "sql_session_start" policy.
		#  See raddb/policy.d/accounting for more details.
		#
		#  You also need to fix the other queries as
		#  documented below.  Look for "sql_session_start".
		#
		post-auth {
			query = "\
				INSERT INTO ${....acct_table1} \
					(${...column_list}) \
				VALUES(\
					'%{Acct-Session-Id}', \
					'%{Acct-Unique-Session-Id}', \
					'%{SQL-User-Name}', \
					NULLIF('%{Realm}', ''), \
					'%{%{NAS-IPv6-Address}:-%{NAS-IP-Address}}', \
					NULLIF('%{%{NAS-Port-ID}:-%{NAS-Port}}', ''), \
					'%{NAS-Port-Type}', \
					${....event_timestamp}, \
					${....event_timestamp}, \
					NULL, \
					0, \
					'', \
					'%{Connect-Info}', \
					NULL, \
					0, \
					0, \
					'%{Called-Station-Id}', \
					'%{Calling-Station-Id}', \
					NULL, \
					'%{Service-Type}', \
					'', \
					NULL, \
					NULL, \
					NULL, \
					NULL, \
					NULL \
					${....class.reply_xlat})"

			query = "\
				UPDATE ${....acct_table1} \
				SET \
					AcctStartTime = ${....event_timestamp}, \
					AcctUpdateTime = ${....event_timestamp}, \
					ConnectInfo_start = '%{Connect-Info}', \
					AcctSessionId = '%{Acct-Session-Id}' \
				WHERE AcctUniqueId = '%{Acct-Unique-Session-Id}' \
				AND AcctStopTime IS NULL"
		}

		start {
			query = "\
				UPDATE wf_radius_session \
				SET \
					\"userName\" = '%{SQL-User-Name}', \
					\"startedAt\" = ${....event_timestamp}, \
					\"lastInterimAt\" = ${....event_timestamp}, \
					status = 'START'::\"RadiusAcctStatus\", \
					\"callingStationId\" = '%{Calling-Station-Id}', \
					\"framedIpAddress\" = NULLIF('%{Framed-IP-Address}', '')::text, \
					\"nasIdentifier\" = NULLIF('%{NAS-Identifier}', ''), \
					\"updatedAt\" = CURRENT_TIMESTAMP \
				WHERE \"acctSessionId\" = '%{Acct-Session-Id}' \
				AND \"nasIpAddress\" = '%{%{NAS-IPv6-Address}:-%{NAS-IP-Address}}' \
				AND \"stoppedAt\" IS NULL"

			query = "\
				INSERT INTO wf_radius_session (\
					id, \"acctSessionId\", \"userName\", status, \
					\"startedAt\", \"lastInterimAt\", \"callingStationId\", \"framedIpAddress\", \
					\"nasIpAddress\", \"nasIdentifier\", \"updatedAt\"\
				) \
				VALUES(\
					gen_random_uuid(), \
					'%{Acct-Session-Id}', \
					'%{SQL-User-Name}', \
					'START'::\"RadiusAcctStatus\", \
					${....event_timestamp}, \
					${....event_timestamp}, \
					'%{Calling-Station-Id}', \
					NULLIF('%{Framed-IP-Address}', '')::text, \
					'%{%{NAS-IPv6-Address}:-%{NAS-IP-Address}}', \
					NULLIF('%{NAS-Identifier}', ''), \
					CURRENT_TIMESTAMP \
				) \
				ON CONFLICT (\"acctSessionId\", \"nasIpAddress\") DO UPDATE SET \
					\"userName\" = EXCLUDED.\"userName\", \
					\"startedAt\" = EXCLUDED.\"startedAt\", \
					\"lastInterimAt\" = EXCLUDED.\"lastInterimAt\", \
					status = EXCLUDED.status, \
					\"callingStationId\" = EXCLUDED.\"callingStationId\", \
					\"framedIpAddress\" = EXCLUDED.\"framedIpAddress\", \
					\"nasIdentifier\" = EXCLUDED.\"nasIdentifier\", \
					\"updatedAt\" = CURRENT_TIMESTAMP \
				WHERE wf_radius_session.\"stoppedAt\" IS NULL"
		}

		interim-update {
			query = "\
				UPDATE wf_radius_session \
				SET \
					\"userName\" = '%{SQL-User-Name}', \
					\"framedIpAddress\" = NULLIF('%{Framed-IP-Address}', '')::text, \
					\"sessionTimeSec\" = %{%{Acct-Session-Time}:-NULL}, \
					\"lastInterimAt\" = ${....event_timestamp}, \
					\"inputBytes\" = (('%{%{Acct-Input-Gigawords}:-0}'::bigint << 32) + \
						'%{%{Acct-Input-Octets}:-0}'::bigint), \
					\"outputBytes\" = (('%{%{Acct-Output-Gigawords}:-0}'::bigint << 32) + \
						'%{%{Acct-Output-Octets}:-0}'::bigint), \
					\"totalBytes\" = (('%{%{Acct-Input-Gigawords}:-0}'::bigint << 32) + \
						'%{%{Acct-Input-Octets}:-0}'::bigint + \
						('%{%{Acct-Output-Gigawords}:-0}'::bigint << 32) + \
						'%{%{Acct-Output-Octets}:-0}'::bigint), \
					status = 'INTERIM'::\"RadiusAcctStatus\", \
					\"nasIdentifier\" = NULLIF('%{NAS-Identifier}', ''), \
					\"updatedAt\" = CURRENT_TIMESTAMP \
				WHERE \"acctSessionId\" = '%{Acct-Session-Id}' \
				AND \"nasIpAddress\" = '%{%{NAS-IPv6-Address}:-%{NAS-IP-Address}}' \
				AND \"stoppedAt\" IS NULL"

			query = "\
				INSERT INTO wf_radius_session (\
					id, \"acctSessionId\", \"userName\", status, \
					\"startedAt\", \"lastInterimAt\", \"sessionTimeSec\", \"inputBytes\", \"outputBytes\", \
					\"totalBytes\", \"callingStationId\", \"framedIpAddress\", \"nasIpAddress\", \
					\"nasIdentifier\", \"updatedAt\"\
				) \
				VALUES(\
					gen_random_uuid(), \
					'%{Acct-Session-Id}', \
					'%{SQL-User-Name}', \
					'INTERIM'::\"RadiusAcctStatus\", \
					TO_TIMESTAMP(${....event_timestamp_epoch} - %{%{Acct-Session-Time}:-0}), \
					${....event_timestamp}, \
					%{%{Acct-Session-Time}:-NULL}, \
					(('%{%{Acct-Input-Gigawords}:-0}'::bigint << 32) + \
						'%{%{Acct-Input-Octets}:-0}'::bigint), \
					(('%{%{Acct-Output-Gigawords}:-0}'::bigint << 32) + \
						'%{%{Acct-Output-Octets}:-0}'::bigint), \
					(('%{%{Acct-Input-Gigawords}:-0}'::bigint << 32) + \
						'%{%{Acct-Input-Octets}:-0}'::bigint + \
						('%{%{Acct-Output-Gigawords}:-0}'::bigint << 32) + \
						'%{%{Acct-Output-Octets}:-0}'::bigint), \
					'%{Calling-Station-Id}', \
					NULLIF('%{Framed-IP-Address}', '')::text, \
					'%{%{NAS-IPv6-Address}:-%{NAS-IP-Address}}', \
					NULLIF('%{NAS-Identifier}', ''), \
					CURRENT_TIMESTAMP \
				) \
				ON CONFLICT (\"acctSessionId\", \"nasIpAddress\") DO UPDATE SET \
					\"userName\" = EXCLUDED.\"userName\", \
					\"framedIpAddress\" = EXCLUDED.\"framedIpAddress\", \
					\"sessionTimeSec\" = EXCLUDED.\"sessionTimeSec\", \
					\"lastInterimAt\" = EXCLUDED.\"lastInterimAt\", \
					\"inputBytes\" = EXCLUDED.\"inputBytes\", \
					\"outputBytes\" = EXCLUDED.\"outputBytes\", \
					\"totalBytes\" = EXCLUDED.\"totalBytes\", \
					status = EXCLUDED.status, \
					\"callingStationId\" = EXCLUDED.\"callingStationId\", \
					\"nasIdentifier\" = EXCLUDED.\"nasIdentifier\", \
					\"updatedAt\" = CURRENT_TIMESTAMP \
				WHERE wf_radius_session.\"stoppedAt\" IS NULL"
		}

		stop {
			query = "\
				UPDATE wf_radius_session \
				SET \
					\"userName\" = '%{SQL-User-Name}', \
					\"stoppedAt\" = ${....event_timestamp}, \
					\"lastInterimAt\" = ${....event_timestamp}, \
					\"sessionTimeSec\" = COALESCE(%{%{Acct-Session-Time}:-NULL}, \
						(${....event_timestamp_epoch} - EXTRACT(EPOCH FROM(\"startedAt\"))))::integer, \
					\"inputBytes\" = (('%{%{Acct-Input-Gigawords}:-0}'::bigint << 32) + \
						'%{%{Acct-Input-Octets}:-0}'::bigint), \
					\"outputBytes\" = (('%{%{Acct-Output-Gigawords}:-0}'::bigint << 32) + \
						'%{%{Acct-Output-Octets}:-0}'::bigint), \
					\"totalBytes\" = (('%{%{Acct-Input-Gigawords}:-0}'::bigint << 32) + \
						'%{%{Acct-Input-Octets}:-0}'::bigint + \
						('%{%{Acct-Output-Gigawords}:-0}'::bigint << 32) + \
						'%{%{Acct-Output-Octets}:-0}'::bigint), \
					\"terminateCause\" = '%{Acct-Terminate-Cause}', \
					\"framedIpAddress\" = NULLIF('%{Framed-IP-Address}', '')::text, \
					status = 'STOP'::\"RadiusAcctStatus\", \
					\"nasIdentifier\" = NULLIF('%{NAS-Identifier}', ''), \
					\"updatedAt\" = CURRENT_TIMESTAMP \
				WHERE \"acctSessionId\" = '%{Acct-Session-Id}' \
				AND \"nasIpAddress\" = '%{%{NAS-IPv6-Address}:-%{NAS-IP-Address}}'"

			query = "\
				INSERT INTO wf_radius_session (\
					id, \"acctSessionId\", \"userName\", status, \
					\"startedAt\", \"lastInterimAt\", \"stoppedAt\", \"sessionTimeSec\", \"inputBytes\", \
					\"outputBytes\", \"totalBytes\", \"callingStationId\", \"terminateCause\", \
					\"framedIpAddress\", \"nasIpAddress\", \"nasIdentifier\", \"updatedAt\"\
				) \
				VALUES(\
					gen_random_uuid(), \
					'%{Acct-Session-Id}', \
					'%{SQL-User-Name}', \
					'STOP'::\"RadiusAcctStatus\", \
					TO_TIMESTAMP(${....event_timestamp_epoch} - %{%{Acct-Session-Time}:-0}), \
					${....event_timestamp}, \
					${....event_timestamp}, \
					NULLIF('%{Acct-Session-Time}', '')::integer, \
					(('%{%{Acct-Input-Gigawords}:-0}'::bigint << 32) + \
						'%{%{Acct-Input-Octets}:-0}'::bigint), \
					(('%{%{Acct-Output-Gigawords}:-0}'::bigint << 32) + \
						'%{%{Acct-Output-Octets}:-0}'::bigint), \
					(('%{%{Acct-Input-Gigawords}:-0}'::bigint << 32) + \
						'%{%{Acct-Input-Octets}:-0}'::bigint + \
						('%{%{Acct-Output-Gigawords}:-0}'::bigint << 32) + \
						'%{%{Acct-Output-Octets}:-0}'::bigint), \
					'%{Calling-Station-Id}', \
					'%{Acct-Terminate-Cause}', \
					NULLIF('%{Framed-IP-Address}', '')::text, \
					'%{%{NAS-IPv6-Address}:-%{NAS-IP-Address}}', \
					NULLIF('%{NAS-Identifier}', ''), \
					CURRENT_TIMESTAMP \
				) \
				ON CONFLICT (\"acctSessionId\", \"nasIpAddress\") DO UPDATE SET \
					\"userName\" = EXCLUDED.\"userName\", \
					\"stoppedAt\" = EXCLUDED.\"stoppedAt\", \
					\"lastInterimAt\" = EXCLUDED.\"lastInterimAt\", \
					\"sessionTimeSec\" = EXCLUDED.\"sessionTimeSec\", \
					\"inputBytes\" = EXCLUDED.\"inputBytes\", \
					\"outputBytes\" = EXCLUDED.\"outputBytes\", \
					\"totalBytes\" = EXCLUDED.\"totalBytes\", \
					\"terminateCause\" = EXCLUDED.\"terminateCause\", \
					\"framedIpAddress\" = EXCLUDED.\"framedIpAddress\", \
					status = EXCLUDED.status, \
					\"callingStationId\" = EXCLUDED.\"callingStationId\", \
					\"nasIdentifier\" = EXCLUDED.\"nasIdentifier\", \
					\"updatedAt\" = CURRENT_TIMESTAMP"
		}

		#
		#  No Acct-Status-Type == ignore the packet
		#
		accounting {
		     query = "SELECT true"
		}
	}
}


#######################################################################
# Authentication Logging Queries
#######################################################################
# postauth_query                - Insert some info after authentication
#######################################################################

post-auth {
	# Write SQL queries to a logfile. This is potentially useful for bulk inserts
	# when used with the rlm_sql_null driver.
#	logfile = ${logdir}/post-auth.sql

	# Log authentication attempt to standard postauth table
	query = "\
		INSERT INTO ${..postauth_table} \
			(username, pass, reply, authdate ${..class.column_name}) \
		VALUES(\
			'%{User-Name}', \
			'%{%{User-Password}:-%{Chap-Password}}', \
			'%{reply:Packet-Type}', \
			'%S.%M' \
			${..class.reply_xlat})"

	# Update credential activation timestamp on successful authentication
	# Only updates if activatedAt is NULL and status allows activation
	query = "\
		UPDATE wf_credential \
		SET \
			\"activatedAt\" = CURRENT_TIMESTAMP, \
			status = CASE \
				WHEN status = 'SOLD' THEN 'ACTIVATED'::\"CredentialStatus\" \
				ELSE status \
			END, \
			\"updatedAt\" = CURRENT_TIMESTAMP \
		WHERE (token = '%{User-Name}' OR username = '%{User-Name}') \
		AND \"activatedAt\" IS NULL \
		AND status IN ('SOLD', 'ACTIVATED', 'ACTIVE', 'NEW') \
		AND \"deletedAt\" IS NULL \
		AND '%{reply:Packet-Type}' = 'Access-Accept'"
}
