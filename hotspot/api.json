{
   "captive": $(if logged-in == 'yes')false$(else)true$(endif),
   "user-portal-url": "https://portal-v2.volowifi.com/portal/auth?mac=$(mac)&ip=$(ip)&nas_ip=$(server-address)&NASID=$(identity)&hostname=$(hostname)&server-name=$(server-name)&link-login=$(link-login-esc)&link-login-only=$(link-login-only-esc)&link-logout=$(link-logout-esc)&link-orig=$(link-orig-esc)",
   "can-extend-session": false
}
