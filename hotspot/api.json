{
   "captive": $(if logged-in == 'yes')false$(else)true$(endif),
   "user-portal-url": "https://portal.volowifi.com/auth?mac=$(mac)&ip=$(ip)&nas_ip=$(hostname)&link-login=$(link-login-esc)&link-login-only=$(link-login-only-esc)&link-logout=$(link-logout-esc)&link-orig=$(link-orig-esc)",
   "can-extend-session": false
}
