import path from 'path';

export const PRISMA_JSON_DIR = path.resolve(__dirname, '../json');

export const MENU_JSON = path.join(PRISMA_JSON_DIR, 'menu.json');
export const ROLE_SETTINGS_JSON = path.join(PRISMA_JSON_DIR, 'role-settings.json');
export const TRANSLATION_THEMES_JSON = path.join(PRISMA_JSON_DIR, 'translation-themes.json');
