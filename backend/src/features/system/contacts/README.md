# Contact support (frontend mirror)

The contacts UI is `frontend/src/features/system/contacts` and reads/writes `support_contacts` via **`system/app-setting`**.

No separate routes module here yet — folder reserved for future dedicated endpoints.
