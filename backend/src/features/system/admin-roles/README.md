# Admin roles (frontend mirror)

The admin roles UI is `frontend/src/features/system/admin-roles`.

API implementation lives in **`core/permissions`** (`/console/menu-permission/*`). Keep permission logic in one place; this folder exists only for layout parity with the frontend.
