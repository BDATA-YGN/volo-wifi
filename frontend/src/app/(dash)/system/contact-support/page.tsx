import { SectionPlaceholder } from "@/app/(dash)/_components/SectionPlaceholder";

export default function Page() {
  return (
    <SectionPlaceholder
      groupKey="system"
      translationKey="menus.contact-support"
      path="/system/contact-support"
    />
  );
}
