import { SectionPlaceholder } from "@/app/(dash)/_components/SectionPlaceholder";

export default function Page() {
  return (
    <SectionPlaceholder
      groupKey="system"
      translationKey="menus.employee-portal-access"
      path="/system/employee-portal-access"
    />
  );
}
