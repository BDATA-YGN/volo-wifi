/** Obscure console entry — not linked from the public welcome page. */
export const CONSOLE_LOGIN_PATH = "/wifi/login";

/** Default landing after console authentication. */
export const CONSOLE_HOME_PATH = "/wifi";

/** Authenticated system guide (former `/` dashboard home). */
export const CONSOLE_GUIDE_PATH = "/home";
