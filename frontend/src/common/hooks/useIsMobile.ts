"use client";

/** @deprecated Import from `@/common/hooks/useBreakpoint` — kept for existing imports. */
export { useIsMobile as default, useIsMobile, BREAKPOINT_MD, BREAKPOINT_LG, useIsTabletDown } from "./useBreakpoint";
